#include <stdio.h>
#include <stdlib.h>

int (fib) (fib) int (fib);
{
    int a = 0,
        b = 1,
        t = 0;
    do {} while (
        t = b,
        b = a+b,
        a = t,
        0 <-- fib
    );
    return a;
}

void main(int argc, char**argv)
{
    int n=
    {
        atoi(argv[1])
    };
    while (printf("The %dth fibonnaci number is %d\n", n, fib(n)), 0);
}
